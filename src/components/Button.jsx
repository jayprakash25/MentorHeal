const Button = () => {
  return (
    <div>
      <button className="border-[1px] hover:bg-black hover:text-white ease-in-out duration-300 font-semibold text-sm border-gray-800 px-5 py-2 rounded-full">
        Get Started
      </button>
    </div>
  );
};

export default Button;
